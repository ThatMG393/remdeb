[host_machine]
system = 'android'
cpu_family = 'arm'
cpu = 'arm'
endian = 'little'

[constants]
ndk = ''
ndk_bin = ndk + 'bin/
toolchain = ndk_bin + 'arm-linux-androideabi-'

[binaries]
c = toolchain + 'gcc'
cpp = toolchain + 'g++'
ar = toolchain + 'ar'
ld = toolchain + 'ld'
objcopy = toolchain + 'objcopy'
strip = toolchain + 'strip'
